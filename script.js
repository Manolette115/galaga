// script base, se espera que el usuario lo reemplace con la versión completa
document.getElementById("startScreen").addEventListener("click", () => {
  document.getElementById("startScreen").classList.add("hide");
});
